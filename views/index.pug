html
  body
    form(action="/charge", method="post")
      article
        label="Amount: " + my_amount.currency + " " + my_amount.amount
      script(
        src="//checkout.stripe.com/v2/checkout.js",
        class="stripe-button",
        data-key=keyPublishable,
        data-locale="auto",
        data-description="Sample Charge",
        data-amount=my_amount.amount*100)
